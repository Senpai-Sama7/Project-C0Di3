import { EventEmitter } from 'events';

export class EventBus extends EventEmitter {
  // This class now inherits all the methods from EventEmitter,
  // ensuring type compatibility across the application.
}
